language: rust
rust:
  - stable
  - beta
  - nightly
jobs:
  allow_failures:
    - rust: nightly
  fast_finish: true

cache: cargo
before_cache:
  - rm -rf "$TRAVIS_HOME/.cargo/registry/src"

script:
  - (cd examples/truffle; npm install; npm run build)
  - cargo build --features example --verbose --all
  - cargo test --features example --verbose --all
  - cargo fmt --all -- --check
  - cargo clippy --all