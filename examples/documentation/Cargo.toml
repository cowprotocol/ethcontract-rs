[package]
name = "examples-documentation"
version = "0.0.0"
publish = false
edition = "2018"
license = "MIT OR Apache-2.0"

[dependencies]
ethcontract = { path = "../../ethcontract", default-features = false, features = ["derive"] }
